syntax = "proto3";

package emojimix;

option go_package="/proto";

// 内部消息，用于表示 map[int]string
message InnerMap {
    map<string, string> inner_map = 1;
}

// 外部消息，用于表示 map[int]InnerMap
message OuterMap {
    map<string, InnerMap> outer_map = 1;
}